import { styled } from 'gatsby-theme-stitches/src/config';

const Body = styled('div', {
  gridArea: 'body',
  display: 'grid',
  alignItems: 'center',
  justifyContent: 'flex-start',
});

export default Body;
